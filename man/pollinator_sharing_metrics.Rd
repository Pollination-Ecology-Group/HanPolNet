% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pollinator_sharing_metrics.R
\name{pollinator_sharing_metrics}
\alias{pollinator_sharing_metrics}
\title{Analyze and Plot Pollinator Sharing Metrics}
\usage{
pollinator_sharing_metrics(
  data,
  compare_by = "year",
  level = "community",
  use_rate = TRUE
)
}
\arguments{
\item{data}{A data frame of interaction data, typically filtered by
`get_interaction_data()`. It must be standardized (contain a `rate` column)
if `use_rate = TRUE`.}

\item{compare_by}{A character string specifying the column name to compare
across (e.g., `"year"`). This column must be numeric.}

\item{level}{A character string specifying the level of analysis, passed to
`calculate_pollinator_sharing()`: `"community"` (the default) or `"species"`.}

\item{use_rate}{A logical value. If `TRUE` (the default), the function will
use the standardized `rate` column. If `FALSE`, it will use raw
`interaction_count`.}
}
\value{
A ggplot object showing the trend of pollinator sharing over time.
}
\description{
This function calculates pollinator sharing metrics (Morisita-Horn similarity)
and visualizes the change over a specified time variable (e.g., year).
}
\details{
The function works by splitting the input data by the `compare_by` variable
(e.g., for each year). For each data subset, it calculates the pollinator
sharing metric using `calculate_pollinator_sharing()`. It then combines the
results and generates a line plot showing the trend over time.
}
